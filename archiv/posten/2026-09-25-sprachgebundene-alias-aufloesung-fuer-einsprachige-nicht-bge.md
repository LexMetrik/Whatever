<!-- @posten
dach: QS-KORPUS
titel: Sprachgebundene Alias-Auflösung für einsprachige Nicht-BGE-Entscheide
anlass: Nach-Verdikt zu e3f874779 (PR #1099)
-->

25.9.2026: Die Sperre von «AIMP» (ITA = IRSG, FRA = IVöB) nimmt bund/bstger/RR_2026_46 (it, Rechtshilfe) das richtige IRSG — der Entscheid nennt das IRSG nur als «AIMP». Bei BGE ist eine sprachgebundene Auflösung nicht sauber (trilinguale Regeste im selben Snapshot), bei einsprachigen eidg./kantonalen Entscheiden wäre sie es (Sprache aus dem Body). Fix: Alias je Sprache nur in Snapshots dieser Body-Sprache auflösen, für BGE je Regeste-Sprachfassung; dann AIMP/OCP/OS entsperren wo eindeutig. Korrektur 25.9.2026 (Messbericht A, alle 5173 Snapshots auf main): der zweite Fund war so nicht haltbar. `adapter-entscheide.ts` Z. 731–734 speist NUR `bgeSachgebietHint` (Sachgebiet-Hinweis aus statutes) — nicht die gespeicherten normKeys. Gespeichert wird Z. 783 (`normKeysVonSnapshot(basis) ∪ normKeysVonSnapshot(aza)`, entscheide-mapping.ts:776), und dieser Teil nimmt den Fliesstext bereits mit, genau wie `--remap` (normtext-entscheide.ts:367). Messung: 5172 von 5173 Snapshots identisch zwischen Nachzug und Remap-Rechnung, die eine Abweichung (bge_152_I_61) ist deklariert (ALT_ERHALTEN_ERWARTET). Nachzug und Remap sind also heute schon angeglichen; offen bleibt nur die Divergenz beim Sachgebiets-Hint selbst (Z. 731, ohne Datum/Gerichtstyp) — gesondert prüfen, kein Angleichungs-Bug.

**Erledigt 2026-09-25:** Commits a3f79a126 (Fix) + b7b831bc6 (Remap) + 090ec8ee1 (Tests), Branch feat/qs-korpus-alias-sprache — nach Rebase auf main (#1130/#1132) b13140a90 + Remap neu + 2d43cdcd8. Zum zweiten Fund (Korrektur oben): der Branch rechnet die BGE-Merge-normKeys dennoch EINMAL über den fertigen Merge (statt basis ∪ aza je Seite mit eigenem Datum/eigener Sprache) — Vereinheitlichung, kein Bestandsbug.
