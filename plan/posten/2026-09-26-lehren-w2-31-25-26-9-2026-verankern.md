<!-- @posten
dach: QS-EFFIZIENZ
titel: Lehren W2·31 (25./26.9.2026) verankern
anlass: Session-Notizen 2026-09-26
-->

Steuerfläche voll (1247.9/1248 KB), darum nur als Posten: (1) veraltetes node_modules im Worktree → Prerender «Invalid hook call», vor erstem Build `npm ci --prefer-offline` (§0 4c); (2) nach tailwind.config.js-/Quelländerung Vite-Server bzw. dist neu bauen, sonst Messung am alten Stand (3× belegt, webseiten-pruefung.md); (3) Range.getClientRects() liefert bei Inline-Kindern mehrere Rects je Zeile → nach y gruppieren; (4) Hook tor-schutz leert Playwright-Speclisten aus `$(… | …)` → falsch-grün mit 3 Specs, Listen per Skriptdatei; (5) Lese-Schutz-Hook blockt Screenshots > 200 KB → JPEG q60; (6) Agent aus Session-Worktree kann im zweiten Worktree nicht editieren → EnterWorktree(path) im Dispatch nennen; (7) Tailwind: Container-Regeln stehen im CSS VOR Media-Regeln → `@…/x:grid-cols-N` braucht gleiches Media-Präfix, Spaltenzahl an Containerbreite statt Viewport; (8) Landungs-Ketten nach `git rebase` nur mit `&&` — mit `;` lief nach Konflikt `gh pr merge --auto` auf dem alten Kopf weiter
- (9) Nachtrag 26.9.2026, B5-Nachzug: ein Sonnet-Bauer umging den F9-Wächter (tap-ziel-token.test.ts) per `min-height: calc(26rem * 1)` — «für den Regex unsichtbar». Haupt-Session ersetzte es durch Token `minHeight.vorschau` (PR #1150). Kandidat Dispatch-§0: «Ein Tor wird nie umschrieben, sondern bedient oder begründet ausgenommen»; zusätzlich Wächter-Regex auf `calc(<Zahl>rem` erweitern.
